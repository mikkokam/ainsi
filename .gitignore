node_modules/
bun.lock
*.html
!samples/*.html
# the studio's start page is source, not a built deck
!src/studio/*.html
.DS_Store

# joined source written by the d2 render script in --watch mode
.*.d2
